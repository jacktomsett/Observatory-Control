{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        // Build tasks
        {
            "label": "Build workspace",
            "detail": "Build ROS 2 workspace",
            "type": "shell",
            "command": "source /opt/ros/kilted/setup.bash && colcon build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc"
        },
        {
            "label": "Build single package",
            "detail": "Build single ROS 2 package",
            "type": "shell",
            "command": "source /opt/ros/kilted/setup.bash && colcon build --packages-select ${input:package}",
            "group": {
                "kind": "build"
            },
            "problemMatcher": "$gcc"
        },
        {
            "label": "Clean workspace",
            "detail": "Remove built code from workspace",
            "type": "shell",
            "command": "rm -r ${workspaceFolder}/{build,install,log}",
            "problemMatcher": []
        },
        {
            "label": "Format active file",
            "detail": "Run amen_uncrustify on active file",
            "type": "shell",
            "command": "source /opt/ros/kilted/setup.bash && ament_uncrustify --reformat ${file}",
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "package",
            "type": "promptString",
            "description": "Package name",
            "default": "demo_nodes_cpp"
        }
    ]
}